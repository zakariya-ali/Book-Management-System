<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - <%= shopName %></title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <% if (user && user.is_admin) { %>
          <li><a href="/books">Manage Books</a></li>
        <% } %>
        <li><a href="/logout">Logout</a></li>
      </ul>
    </nav>
  </header>
  <main class="container">
    <h1>Welcome, <%= user.first_name %>!</h1>
    <% if (user.is_admin) { %>
      <p><a class="button" href="/books">Go to Book Management</a></p>
    <% } else { %>
      <p>Browse and search for books to purchase.</p>
      <p><a class="button" href="/search">Search Books</a></p>
    <% } %>
  </main>
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> <%= shopName %></p>
  </footer>
</body>
</html>